# このリポジトリで作業するときに、最初に知っておくこと

ADRC（アジア防災センター）荒木田さんの業務自動化。**説明はすべて日本語で行う。**

ここは「毎回どのセッションでも踏み直している事故」を集めた場所。
詳しい経緯は `skills/disaster-report/references/environment.md` にある。

---

## 1. 相手の環境は Windows

| 項目 | 実際 |
|---|---|
| OS | Windows。コンソールは `cmd`、既定コードページは **cp932** |
| bash | Git Bash はあるが、**バッチから呼ぶ前提にしない** |
| Python | あり。`pdfplumber` あり。**`pdftotext`（poppler）は無い** |
| LibreOffice | あり（`soffice`）。PDF変換に使う |
| OneDrive | `C:\Users\arakida\OneDrive - adrc.asia\` 配下。**デスクトップもここにリダイレクトされている** |
| node_modules | `C:\Users\arakida\` に置く。OneDrive配下に作ると数千ファイルが同期対象になる |

**クラウドのセッションから、この人のPCやOneDriveへは書き込めない。**
書き込む必要がある成果物は、PC側で動く仕組みを用意するか、セッションへ届けるかのどちらか。

## 2. 改行コードで2回事故を起こしている

**方向が逆の2つがある。両方おさえること。**

| 対象 | 正しい改行 | 間違えるとどうなるか |
|---|---|---|
| `.bat` | **CRLF**（＋ASCIIかcp932） | LF や UTF-8 だと **何も表示せずに終わる**。原因が見えない |
| `.js` `.py` などソース | **LF** | Git for Windows は既定 `core.autocrlf=true` で **CRLF に変換して取り出す**。行末に `\n` を置いた正規表現が0件になる |

2026-08-27、2つ目でビルドが止まった。`gen_deck.js` を CRLF で取り出したせいで
パッチの照合が外れ、しかも「ファイルが改修されている」という趣旨のメッセージが出るため
原因を取り違えやすい。**クラウド（LF）では絶対に再現しない。**

`.gitattributes` で `* text=auto eol=lf` / `*.bat text eol=crlf` を固定してある。
ファイルを追加するときはこれを崩さないこと。

## 3. 定期タスク（cron / Routines）の2つの落とし穴

- **定期タスクは毎回まっさらなセッションで `main` を clone して始まる。**
  作業ブランチは引き継がれない。スキル一式が `main` に無い間は、
  プロンプトの冒頭で `git fetch` → `git checkout` → **存在確認**まで書く。
  checkout だけ書くと、失敗しても気づかず先へ進む
- **MCP の `create_trigger` から作った定期タスクにはコネクタが付かない。**
  Superhuman などを使うタスクは **claude.ai の Routines 画面**で作る。
  戻り値の警告文が唯一の手がかりで、タスク自体は正常に作られたように見える

## 4. 進め方について、はっきり言われていること

- **コマンドを貼らせて、結果をスクショで報告させる往復をしない。**
  それは自動化ではない。1コマンドで完結する形を先に渡す
- **AI側でできることと、人がPCでやらなければならないことを分けて示す。**
  混ぜて並べない
- 失敗の可能性がある手順を渡すときは、**ログをファイルに残す**。
  読んで伝えてもらうのではなく、ファイルを送ってもらえば済むようにする
- **「ビルドが通った」は検証にならない。** ここで出た不具合はいずれも例外を出さなかった
  （空欄のまま出力、画像が無いまま出力、他イベントの画像を掴む）
- **通称でフォルダを呼ばない。** この環境には名前だけでは区別できない場所が複数ある。
  やり取りでは常にフルパスを書く
- わかったことは記録に残して次に使う。ここか `references/` に書く

## 5. 災害レポートの構成（触る前に）

- **熊本はこのまま、海外は統一版**（`skills/disaster-report/`）
- ジェネレータ本体は書き換えない。`generator/gen_deck.base.js` は**素のまま保つ**。
  改修は `scripts/apply_*.js` のパッチ側に書き、`apply_all.js` の一覧にも足す
- ビルドは `node skills/disaster-report/generator/scripts/build_event.js <GLIDE>`
- 送信の前に `resolve_event.js` を必ず通す。数値急変ゲートに掛かったら送らない
